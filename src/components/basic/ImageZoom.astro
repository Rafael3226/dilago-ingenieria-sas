---
import { Image } from "astro:assets";
let { src, alt = "", height, width } = Astro.props;
---

{
  src && (
    <div class="size3 bo-rad-10 hov-img-zoom m-l-r-auto">
      <Image src={src} alt={`IMAGEN: ${alt}`} height={height} width={width} />
    </div>
  )
}
